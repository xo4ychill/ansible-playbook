# =============================================================================
# host_vars/clickhouse.yml – Хост-специфичные переменные
# Назначение: изоляция IP-адресов и явное указание ОС для корректной работы when-условий
# =============================================================================

# 🌐 Единственное место в проекте, где хранится IP. Легко менять при миграции.
ch_host_ip: "89.169.141.201"

# 🐧 Явное указание семейства ОС (полезно при работе с legacy или нестандартными образами)
# ansible_os_family: "RedHat"
# ansible_distribution: "AlmaLinux"
# ansible_distribution_version: "9"
