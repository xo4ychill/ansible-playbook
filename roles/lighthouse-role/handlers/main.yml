# =============================================================================
# roles/lighthouse/handlers/main.yml — Обработчики для Lighthouse
# =============================================================================
---
- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
    daemon_reload: true
  listen: "reload nginx"
