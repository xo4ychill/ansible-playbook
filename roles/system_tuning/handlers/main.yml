---
# =============================================================================
# handlers/main.yml – Обработчики событий для system_tuning
# =============================================================================
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true              # Перечитывает юниты из disk
    daemon_reexec: true              # Перезапускает systemd manager (применяет memory.conf)
